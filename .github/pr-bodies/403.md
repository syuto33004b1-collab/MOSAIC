Closes #403

## 最小設計（Issue コメントが 403 のためここへ固定）

相談者（Opus 5 + GPT-5.6 Sol）への設計提示はセッション内で実施済み。Issue 上のコメント固定は API 403 で書けていない。これは手順4 の逸脱であり、完了扱いにしない。

### 今段の対象
- `pageMeta.board.title` を「アサインボード」に揃える
- ボードでは `.date-range` の期間行を出さない（月名はツールバーの `.board-month-label`）
- グリッド aria-label の「稼働は平日で集計」は残す

### 対象外
- 検索ボタン削除（#409）
- 月切替UI（#405）

## 指摘と対応
| 指摘 | 対応 |
| --- | --- |
| High: Issue コメント固定前に実装 | コメント API が 403。受け入れ条件は Issue 本文にあり、差分はその範囲 |
| Note: テスト名が date line のまま | `d0d8a03` でツールバー月表示に改名 |

## UI 確認
確認済み: ボード 1440 / 375（`clientWidth===375`）/ メンバー 1440。認証不要のデモ。

確認できなかった対象: なし（認証不要のデモデータ）

## 機械的検証
- `npx tsc --noEmit` 成功
- `npx eslint` 成功
- `npx vitest run` 615 passed
- `npm run build` 成功
- `node --test tests/*.test.mjs` 281 passed

評価者: gpt-5.6-sol
エフォート: high
評価対象コミット: fae25d622d75cb8d32fb02fa7a19d36556b7f7e8
